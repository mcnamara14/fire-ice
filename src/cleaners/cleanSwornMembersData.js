export const cleanSwonHouseMembersData = (membersData) => {
  const { name, died } = membersData;
  console.log(membersData)
  // const alive = died ? died : 'Alive';

  // return {
  //   name : alive 
  // }


}